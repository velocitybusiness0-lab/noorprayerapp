# Superwall (paywalls)
# Paste keys from https://superwall.com — never commit production secrets.
# Used by SuperwallApiKeyResolver via Expo public env / app.json extra.
EXPO_PUBLIC_SUPERWALL_IOS_API_KEY=pk_replace_me
EXPO_PUBLIC_SUPERWALL_ANDROID_API_KEY=pk_replace_me

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/prayer_block_background"
  android:orientation="vertical"
  android:padding="32dp">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
      android:id="@+id/prayer_block_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginBottom="12dp"
      android:gravity="center"
      android:text="@string/prayer_block_title"
      android:textColor="@color/prayer_block_text"
      android:textSize="32sp"
      android:textStyle="bold" />

    <TextView
      android:id="@+id/prayer_block_reminder"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginBottom="24dp"
      android:gravity="center"
      android:lineSpacingExtra="4dp"
      android:text="@string/prayer_block_reminder"
      android:textColor="@color/prayer_block_muted"
      android:textSize="16sp" />

    <TextView
      android:id="@+id/prayer_block_quote"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:gravity="center"
      android:lineSpacingExtra="6dp"
      android:paddingHorizontal="8dp"
      android:textColor="@color/prayer_block_text"
      android:textSize="18sp" />
  </LinearLayout>

  <Button
    android:id="@+id/prayer_block_dismiss"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:backgroundTint="@color/prayer_block_accent"
    android:minHeight="56dp"
    android:text="@string/prayer_block_dismiss"
    android:textColor="@color/prayer_block_button_text"
    android:textSize="16sp"
    android:textStyle="bold" />
</LinearLayout>

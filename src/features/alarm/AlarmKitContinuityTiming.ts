/** Delay before re-firing AlarmKit after an early stop/X. */
export const ALARM_KIT_REARM_DELAY_MS = 1_200;

/** Ignore duplicate rearm attempts within this window. */
export const ALARM_KIT_REARM_COOLDOWN_MS = 2_500;

/** Routes for post-paywall permission onboarding. */
export class OnboardingPermissionRoutes {
  static readonly permissions = "/onboarding/permissions" as const;
  static readonly tryForFree = "/onboarding/try-for-free" as const;
}
